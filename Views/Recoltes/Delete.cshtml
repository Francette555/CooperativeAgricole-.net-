@model tedd.Models.Recolte
@{
    ViewData["Title"] = "Supprimer une récolte";
}

<div class="container-fluid">
    <h1 class="mb-4 text-danger">@ViewData["Title"]</h1>

    <div class="alert alert-warning">
        <i class="fas fa-exclamation-triangle"></i>
        Êtes-vous sûr de vouloir supprimer cette récolte ? Le stock sera également ajusté.
    </div>

    <div class="row">
        <div class="col-md-6">
            <div class="card shadow">
                <div class="card-body">
                    <dl class="row">
                        <dt class="col-sm-4 fw-bold">Producteur</dt>
                        <dd class="col-sm-8">@Model.Producteur?.NomComplet</dd>

                        <dt class="col-sm-4 fw-bold">Produit</dt>
                        <dd class="col-sm-8">@Model.TypeProduit?.Nom</dd>

                        <dt class="col-sm-4 fw-bold">Quantité</dt>
                        <dd class="col-sm-8">@Model.Quantite.ToString("N2") @Model.TypeProduit?.Unite</dd>

                        <dt class="col-sm-4 fw-bold">Qualité</dt>
                        <dd class="col-sm-8">@Model.Qualite</dd>

                        <dt class="col-sm-4 fw-bold">Date de récolte</dt>
                        <dd class="col-sm-8">@Model.DateRecolte.ToString("dd/MM/yyyy HH:mm")</dd>
                    </dl>

                    <form asp-action="Delete">
                        <input type="hidden" asp-for="Id" />
                        <button type="submit" class="btn btn-danger">
                            <i class="fas fa-trash"></i> Supprimer
                        </button>
                        <a asp-action="Index" class="btn btn-secondary">Annuler</a>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>